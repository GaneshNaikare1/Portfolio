Portfolio site scaffold for Ganesh Vijay Naikare

Files generated:
- index.html
- style.css
- script.js
- assets/images/avatar.svg (placeholder)
- assets/resume.pdf (placeholder — replace with real resume)

To preview:
- Open `portfolio-app/index.html` in your browser, or
- Start a local server and visit http://localhost:5000 (Python) or http://localhost:5001 (npm)

```powershell
# from workspace root (Python)
python -m http.server 5000 --directory "portfolio-app"

# or using npm (recommended):
cd "portfolio-app"
npm install
npm start
# site will be served at http://localhost:5001
```

Notes:
- Replace `assets/resume.pdf` with a real PDF resume.
- Update LinkedIn/GitHub links in the HTML.
- This is a static, framework-free template using HTML/CSS/vanilla JS.